// can be customized when needed an interceptor in the future
// but for now i wil just export the base url

export const API_URL = process.env.NEXT_API_URL || 'http://localhost:3000' // uses a fallback
